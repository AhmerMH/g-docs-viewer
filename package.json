{
  "name": "gdoc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "build": "mkdir dist && cp public/index.html dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.0.0-beta.2",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
